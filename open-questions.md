# Open Questions

Unresolved project-level questions (naming, hosting, etc.) that don't block current work but shouldn't be forgotten. Remove an entry once it's decided — fold the decision into the doc it actually belongs in (`way-of-working.md`, `brand-guidelines.md`, etc.), don't leave a stale answer here.

- **Real family roster — in progress** — `shared/data/people.json` now has the first 8 of the ~27-person roster (Fletcher & Stella down through David S & Rachel, one direct line), and `timeline/data/travel.json` has real travel/room data for David S and Rachel. This is real family data, not test/placeholder data — keep it as the remaining ~19 people and their travel/room entries are added over time. `timeline/data/meals.json` and `timeline/data/activities.json` are still empty. `shared/data/structures.json` and `shared/data/vehicles.json` were already filled in with the real known locations/vehicles (see `requirements/public.md` → *Structures* / *Vehicles*).
